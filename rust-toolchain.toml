[toolchain]
channel = "1.96.1"
components = ["rustfmt", "clippy"]

# No `targets`. Nothing cross-compiles: dist gives each release target a runner
# of its own architecture, so every build wants the host standard library and
# no other. Listing the four here would cost every job a standard library per
# target it never links.
